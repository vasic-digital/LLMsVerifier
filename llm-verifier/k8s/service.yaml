apiVersion: v1
kind: Service
metadata:
  name: llm-verifier-service
  namespace: llm-verifier
  labels:
    app: llm-verifier
spec:
  selector:
    app: llm-verifier
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
