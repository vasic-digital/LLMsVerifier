# API Keys - NEVER COMMIT
api_keys.json
*_api_keys.json
*_secrets.json
*.env

# Logs (may contain sensitive information)
# Note: Challenge logs can be versioned if they don't contain actual API keys in full
# This configuration allows versioning of results while protecting keys
